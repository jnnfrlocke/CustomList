using System;
using System.Collections.Generic;
using System.Collections;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace CustomList 
{
    
        //public class CustomList<T> : IEnumerable<T>
    //{
            //T[] items;

            //public int index = 0;

        //IEnumerator GetEnumerator();

        //public int Add(T itemToAdd) //to increase count, take the current array and create a new array (first array+ new addition) with bigger capacity???
        //{
        //    for (int index = 0; index < this.Length; index++)
        //    {
        //        if (index[] == null)
        //            itemToAdd = this.index + 1;
        //    }




            //items[0] = itemToAdd; //will set later to find the last item in list and add new item after that
            //return items[0];


                    //for (int index = 0; index < this.Length; index++)
                    //{
                    //    if (index == this.Length)
                    //    {
                    //        itemToAdd = this.Length + 1;
                    //    }
                    //} Can't use length - it could be 8 and the count could be 6

                    //TODO:add indexer

                    //Capacity - to increase capacity, take the current array and create a new array (first array+ new addition/capacity) with bigger capacity???
                    //Numbers.Capacity = 1000];
                    //int num = 1;
                    //for (int i = 0; i < 1001; i++)
                    //{
                    //    numbers.add(num);
                    //    num++;
                    //}

                    //OVERRIDE .TOSTRING ONLINE EXAMPLE
                    //    class Person
                    //{
                    //    public string Name { get; set; }
                    //    public int Age { get; set; }

                    //    public override string ToString()
                    //    {
                    //        return "Person: " + Name + " " + Age;
                    //    }
                    //}

        }

        //IEnumerator IEnumerable<T>.GetEnumerator()
        //{
        //    throw new NotImplementedException();
        //}


        //Use ARRAYS (temp)


    //}

    //}

